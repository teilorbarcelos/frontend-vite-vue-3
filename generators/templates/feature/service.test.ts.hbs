import { describe, it, expect, vi, beforeEach } from 'vitest';
import { api } from '@/lib/axios';
import { {{camelCase name}}Service } from '../{{kebabCase name}}.service';

vi.mock('@/lib/axios', () => ({
  api: {
    get: vi.fn(),
    post: vi.fn(),
    put: vi.fn(),
    delete: vi.fn(),
    patch: vi.fn(),
  },
}));

describe('{{pascalCase name}}Service', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('should fetch {{camelCase name}}s', async () => {
    const mockData = { items: [], total: 0 };
    vi.mocked(api.get).mockResolvedValueOnce({ data: mockData });

    const result = await {{camelCase name}}Service.get{{pascalCase name}}s({ page: 0, size: 10 });

    expect(api.get).toHaveBeenCalledWith('/v1/{{kebabCase name}}', expect.any(Object));
    expect(result).toEqual(mockData);
  });

  it('should fetch a single {{camelCase name}}', async () => {
    const mockData = { id: '1', name: 'Test' };
    vi.mocked(api.get).mockResolvedValueOnce({ data: mockData });

    const result = await {{camelCase name}}Service.get{{pascalCase name}}('1');

    expect(api.get).toHaveBeenCalledWith('/v1/{{kebabCase name}}/1');
    expect(result).toEqual(mockData);
  });
});
