<script setup lang="ts">
import FilterDrawer from '@/components/ui/FilterDrawer.vue';
import { {{constantCase name}}_FILTER_CONFIG } from '../constants/{{kebabCase name}}.constants';

interface Props {
  isOpen: boolean;
  initialValues?: Record<string, unknown>;
}

defineProps<Props>();
const emit = defineEmits(['close', 'filter']);
</script>

<template>
  <FilterDrawer
    :is-open="isOpen"
    :fields="{{constantCase name}}_FILTER_CONFIG"
    :initial-values="initialValues"
    @close="emit('close')"
    @filter="emit('filter', $event)"
  />
</template>
